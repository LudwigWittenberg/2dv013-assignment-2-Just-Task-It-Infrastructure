name: Terraform Bootstrap

on:
  workflow_dispatch:

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    environment: terraform
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GOOGLE_CREDENTIALS }}

      - name: Set up ENV Variables
        env:
          GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
          GCP_REGION: ${{ vars.GCP_REGION }}
          GCP_PROJECT_NAME: ${{ vars.GCP_PROJECT_NAME }}
        run: |
          echo "TF_VAR_project_id=${GCP_PROJECT_ID}" >> $GITHUB_ENV
          echo "TF_VAR_region=${GCP_REGION}" >> $GITHUB_ENV
          echo "TF_VAR_project_name=${GCP_PROJECT_NAME}" >> $GITHUB_ENV
          echo "TF_VAR_initial_service_account_email=${{ vars.GITHUB_SERVICE_ACCOUNT_EMAIL }}" >> $GITHUB_ENV

      - name: Terraform init
        run: terraform init
        working-directory: Terraform/bootstrap

      - name: Terraform plan
        run: terraform plan -out=tfplan
        working-directory: Terraform/bootstrap

      - name: Terraform apply
        run: terraform apply -auto-approve tfplan
        working-directory: Terraform/bootstrap

      - name: Capture outputs
        run: |
          mkdir -p /tmp/credentials
          terraform output -raw tfstate_bucket > /tmp/credentials/bucket_name.txt
          terraform output -raw tf_sa_key_json > /tmp/credentials/sa_key.json
        working-directory: Terraform/bootstrap

      - name: Upload bucket name artifact
        uses: actions/upload-artifact@v4
        with:
          name: terraform-bucket-name
          path: /tmp/credentials/bucket_name.txt
          retention-days: 30

      - name: Upload service account key artifact
        uses: actions/upload-artifact@v4
        with:
          name: terraform-sa-key
          path: /tmp/credentials/sa_key.json
          retention-days: 7
